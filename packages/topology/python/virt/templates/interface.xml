<interface type="${interface-type}">
  <mac address="${mac-address}"/>
  <source address="${udp-dest-address}" port="${udp-dest-port}">
    <local address="${udp-source-address}" port="${udp-source-port}"/>
  </source>
  <source network="${network}"/>
  <source bridge="${bridge}"/>
  <target dev="${dev}"/>
  <model type="${model-type}"/>
</interface>
